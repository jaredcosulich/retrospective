.vote
  .float-right= link_to('Delete', board_item_vote_path(@board, @item, vote), method: :delete, data: {confirm: 'Are you sure you want to delete this vote?'})
  - if vote.user.blank?
    Voted Up By Anonymous
  - else
    Voted Up
    = vote.user.name
